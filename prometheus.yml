global:
  scrape_interval: 5s # Как часто будут собираться метрики

scrape_configs:
  - job_name: "fastapi-apps"
    metrics_path: "/metrics"
    static_configs:
      # Тут должны быть имена приложений из docker-compose
      - targets: ["app1:8080", "app2:8080", "app3:8080"]
